<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Aplication.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Wsiąść do pociągu">

    <Grid x:Name="RootLayout">

        <!-- Plansza (MapBoardView) jest wstawiana w kodzie po wczytaniu danych. -->

        <ActivityIndicator
            x:Name="LoadingIndicator"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            IsRunning="True" />

        <!-- Krótka legenda: interakcja i gesty widoku mapy. -->
        <Border
            Margin="12"
            Padding="10,6"
            HorizontalOptions="Start"
            VerticalOptions="Start"
            BackgroundColor="#CC000000"
            StrokeThickness="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <Label
                FontSize="11"
                TextColor="White"
                Text="Tap miasta = oznacz/odznacz • Tap trasy = zaznaczona→wykonana→reset • Pinch = zoom • Pan = przesuń" />
        </Border>

        <!-- Liczniki wagonów: zaznaczone / wykonane trasy. -->
        <Border
            Margin="12"
            Padding="10,6"
            HorizontalOptions="End"
            VerticalOptions="Start"
            BackgroundColor="#CC000000"
            StrokeThickness="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="8" />
            </Border.StrokeShape>
            <Label
                x:Name="CountersLabel"
                FontSize="11"
                TextColor="White" />
        </Border>

    </Grid>

</ContentPage>
